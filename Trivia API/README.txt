mayilen hanumunthadu
101107011
npm install path
npm install express
npm install uuid
to run
node server.js 

for the get session enter a limit, 
category which refers to the category_id,
token which refers to the session ids that been created.
get questions
the server would return a status 0 if the questions requestsed are valid
server returns a status 1 if not enough or 0 if token is unvaild 

get sessions
returns an array containing all sessions 
post sessions 
creates a session and displays id on screen
delete session 
remove the session 


